# Gradle
org.gradle.jvmargs=-Xmx3G
org.gradle.parallel=true

# Minecraft / Fabric 版本（改这里即可升级版本）
minecraft_version=1.21.8
yarn_mappings=1.21.8+build.1
loader_version=0.17.2
loom_version=1.11.8
fabric_version=0.133.4+1.21.8

# Kotlin 版本实际写死在 build.gradle 里（避免属性没同步导致配置阶段失败），
# 这一行只作记录，改版本时两处要一起改。
kotlin_version=2.1.20

# 模组信息
mod_id=clientmod
mod_name=ClientMod
mod_version=1.0.0
maven_group=com.example
archives_base_name=clientmod
